resources:
  pipelines:
  - pipeline: alexangasweb
    source: alexangas.alexangas-web-ci
    trigger: 
      branches:
      - master

jobs:
  - job: DeployLinux
    pool: Default
    steps:
      - template: ./_pwsh.yml
