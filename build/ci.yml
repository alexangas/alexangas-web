trigger:
  - master

variables:
  npm_config_cache: $(Pipeline.Workspace)/.npm

stages:
  - stage: BuildStage
    jobs:
      - job: Build
      - template: ./_build.yml
        parameters:
          publish: false

  - stage: UnitTest
    jobs:
      - job: Test
      - template: ./_unit-tests.yml
