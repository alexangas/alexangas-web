pool:
  vmImage: "ubuntu-latest"

jobs:
  - job: RunCI
    steps:
      - template: ./_install.yml

      - template: ./_build.yml
        parameters:
          publish: false

      - template: ./_unit-tests.yml
