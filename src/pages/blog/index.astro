---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en" class="theme-light">
	<head>
		<BaseHead title="Blog" description="Recent blog posts by Alex Angas." />
	</head>
	<body>
		<Header />
		<main class="container">
			<section class="section">
				<h1 class="title">
					Blog
				</h1>
				<p class="pb-5">
					Blog posts come and blog posts go...
					I only keep recent posts that still get traffic, and usually can't write about what I'm working on. 🤷
				</p>
				<p class="pb-5">
					Here's what we have right now:
				</p>
					{
						posts.map((post) => (
							<div class="box">
								<article class="media">
									{post.data.heroImage && 
										<div class="media-left">
											<figure class="image is-128x128">
												<Image src={post.data.heroImage} alt="Hero" width="128" height="128" loading="eager" decoding="sync" />
											</figure>
										</div>
									}
									<div class="media-content">
										<div class="content">
											<p class="subtitle mb-1">
												<a href={`/blog/${post.id}/`}>{post.data.title}</a>
											</p>
											<p class="mb-1">
												{post.data.description}
											</p>
											<p>
												<FormattedDate date={post.data.pubDate} />
											</p>
										</div>
									</div>
								</article>
							</div>
						))
					}
			</section>
		</main>
		<Footer />
	</body>
</html>
